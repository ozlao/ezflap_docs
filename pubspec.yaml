name: ezflap_docs
description: ezFlap documentation and examples.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.9.0+9

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.1.4
  build_runner: ^2.0.6
  intl: ^0.17.0

  ezflap: ^0.9.0              # forRelease

  ezflap_widgets: ^0.9.0      # forRelease

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
